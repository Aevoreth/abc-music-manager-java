/**
 * Persistence for ABC Music Manager: SQLite (Python-compatible v12) and {@code preferences.json}.
 * Repositories share a {@link SqliteDatabase}; only the session owner closes it.
 */
package com.aevoreth.abcmm.storage;
